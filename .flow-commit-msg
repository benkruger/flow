Add Rust post-compact hook with tests for PostCompact event handling.

tl;dr

Ports lib/post-compact.py to Rust as src/hooks/post_compact.rs,
preserving the exact state mutation contract (compact_summary,
compact_cwd, compact_count increment). Python deletion deferred
to final cleanup task.

- src/hooks/mod.rs: New hooks module for hook entry points
- src/hooks/post_compact.rs: capture_compact_data() + run() + 9 tests
- src/lib.rs: Register hooks module
