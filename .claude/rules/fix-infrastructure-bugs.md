# Fix Infrastructure Bugs Immediately

When a critical infrastructure bug is discovered during a workflow
(locks, state files, hooks, phase gates), stop and pivot to fixing
it immediately. Never suggest force-releasing, working around, or
deferring the fix to continue the current task.

Infrastructure bugs affect every engineer and every flow. The bug
IS the current task now.
