#!/usr/bin/env bash
set -euo pipefail

# Fast fail: formatting check is instant
cargo fmt --check

# Clippy with all targets — compiles everything including tests
cargo clippy --all-targets --quiet -- -D warnings

# Tests reuse clippy's compilation artifacts; nextest runs binaries in parallel
cargo nextest run --status-level none --final-status-level fail
