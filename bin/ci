#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"

# Build Rust binary before running tests
if [ -f "$REPO_ROOT/Cargo.toml" ]; then
  cargo build --quiet
fi

cargo test --quiet
cargo clippy --quiet
cargo fmt --check
