#!/usr/bin/env bash
# FLOW formatter — called by `bin/flow ci` (and `bin/flow ci --format`).
# Auto-fixes in place. Returns nonzero only on cargo fmt failures
# (parse errors, rustfmt.toml issues).
set -euo pipefail
exec cargo fmt
