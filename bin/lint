#!/usr/bin/env bash
# FLOW linter — called by `bin/flow lint` and `bin/flow ci`.
# Treats every clippy warning as an error so CI gates on lint cleanliness.
set -euo pipefail
export RUSTFLAGS="${RUSTFLAGS:-} -D warnings"
exec cargo clippy --all-targets -- -D warnings
