#!/usr/bin/env bash
# FLOW-STUB-UNCONFIGURED (remove this line once you configure a real format checker)
# FLOW format checker — called by `bin/flow format` and `bin/flow ci`.
#
# `bin/flow ci` runs format → lint → build → test in sequence; format
# is first because it is the cheapest fail-fast check. This stub does
# nothing yet — uncomment one of the lines below for your toolchain
# and delete the rest. The exit-0 default keeps fresh primes from
# blocking CI; you will see the reminder on every CI run until you
# configure a real format checker.
#
# Examples:
#   exec cargo fmt --check
#   exec ruff format --check .
#   exec gofmt -l .
#   exec rubocop --only Layout
#   exec npx prettier --check .
#   exec swift-format lint --recursive Sources/
#   exec black --check .

echo "bin/format: no format checker configured (stub) — edit $0" >&2
exit 0
