#!/usr/bin/env bash
# FLOW-STUB-UNCONFIGURED (remove this line once you configure a real linter)
# FLOW linter — called by `bin/flow ci` (and `bin/flow ci --lint`).
#
# Treat every warning as a CI failure so the gate stays meaningful.
# This stub does nothing yet — uncomment one of the lines below for
# your toolchain and delete the rest. The exit-0 default keeps fresh
# primes from blocking CI; you will see the reminder on every CI run
# until you configure a real linter.
#
# Examples:
#   exec cargo clippy --all-targets -- -D warnings
#   exec ruff check .
#   exec go vet ./...
#   exec rubocop
#   exec npx eslint .
#   exec swiftlint --strict

echo "bin/lint: no linter configured (stub) — edit $0" >&2
exit 0
